#include<bits/stdc++.h>
using namespace std;

int main() {
    int N; // deklaracija varijable N koja nije inicijalizirana
    //cout << N; // ne mogu utvrditi vrijednost varijable N jer nije inicijalizirana
    cin >> N;
    cout << ++N << endl;
    // N++ isto ++N isto N+=1 isto N=N+1
    return 0;
}